/**
 * TODO: get the student's compared files from the backend
 * @param student
 * @returns {JSX.Element}
 */
function getStudentSimilarity(student) {
    return(
        <p>{student}</p>
    );
}
export default getStudentSimilarity